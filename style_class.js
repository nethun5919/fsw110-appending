const h2 = ["Yogurt", "Banana", "Peaches", "Mango", "Turkey"];
 

for (let i = 0;i<h2.length; i++){
  console.log(grocerylist[i]);

  let H2 = document.createElement("list");
  h2 = innerText = grocerylist[i];
      grocerylist.appendChild(h2)
}